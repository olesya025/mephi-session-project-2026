    1  ip link show
    2  nmcli connection show
    3  sudo nmcli connection modify enp0s8 con-name "Mephi"
    4  sudo nmcli connection modify "Mephi" ipv4.method manual ipv4.addresses 192.168.1.100/24
    5  sudo nmcli connection modify "Mephi" ipv4.gateway 192.168.1.1
    6  sudo nmcli connection modify "Mephi" ipv4.dns 8.8.8.8
    7  sudo nmcli connection modify "Mephi" connection.autoconnect yes
    8  sudo nmcli connection up "Mephi"
    9  ip addr show enp0s8
   10  ip route show
   11  sudo hostnamectl set-hostname mephi-2026.domain.local
   12  hostnamectl
   13  ping -c 4 8.8.8.8 > /tmp/network_check.txt
   14  ping -c 4 192.168.1.1 >> /tmp/network_check.txt
   15  cat /tmp/network_check.txt
   16  sudo dnf install -y nginx tcpdump libcap-ng-utils
   17  rpm -q nginx tcpdump libcap-ng-utils
   18  cd /tmp
   19  sudo dnf download tcpdump --destdir=/tmp
   20  ls -la /tmp/tcpdump*.rpm
   21  sudo rpm -ivh /tmp/tcpdump*.rpm --replacepkgs
   22  tcpdump --version
   23  lsblk
   24  sudo fdisk /dev/sdb
   25  sudo mkfs.ext4 -L MEPHI_DATA /dev/sdb1
   26  sudo mkdir -p /data/mephi-web
   27  echo "LABEL=MEPHI_DATA /data/mephi-web ext4 defaults 0 2" | sudo tee -a /etc/fstab
   28  sudo mount -a
   29  df -h /data/mephi-web
   30  sudo systemctl start nginx
   31  sudo systemctl enable nginx
   32  sudo journalctl -u nginx --since "5 minutes ago" > /tmp/nginx_recent_logs.txt
   33  cat /tmp/nginx_recent_logs.txt
   34  sudo useradd -m mephi-admin
   35  echo "mephi-admin:P@ssw0rd2026" | sudo chpasswd
   36  sudo groupadd mephi-devs
   37  sudo usermod -aG mephi-devs mephi-admin
   38  id mephi-admin
   39  getent group mephi-devs
   40  sudo chown -R mephi-admin:mephi-devs /data/mephi-web
   41  sudo chmod 2775 /data/mephi-web
   42  ls -ld /data/mephi-web
   43  getenforce
   44  sudo semanage fcontext -a -t httpd_sys_content_t "/data/mephi-web(/.*)?"
   45  sudo restorecon -Rv /data/mephi-web
   46  ls -Z /data/mephi-web
   47  sudo setcap cap_net_raw,cap_net_admin+ep /usr/sbin/tcpdump
   48  getcap /usr/sbin/tcpdump
   49  echo "Hello from Student: M2551024" | sudo tee /data/mephi-web/index.html
   50  cat /data/mephi-web/index.html
   51  curl http://localhost
   52  curl http://192.168.1.100
   53  echo "root" | sudo tee /etc/ssh/denied_users
   54  echo "auth required pam_listfile.so item=user sense=deny file=/etc/ssh/denied_users onerr=succeed" | sudo tee -a /etc/pam.d/sshd
   55  echo "auth required pam_listfile.so item=user sense=deny file=/etc/ssh/denied_users onerr=succeed" | sudo tee -a /etc/pam.d/login
   56  sudo systemctl restart sshd
   57  cp /tmp/network_check.txt ~/
   58  cp /tmp/nginx_recent_logs.txt ~/
   59  cp /etc/fstab ~/fstab.txt
   60  getenforce > ~/selinux_status.txt
   61  ls -Zd /data/mephi-web > ~/file_contexts.txt
   62  getcap /usr/sbin/tcpdump > ~/tcpdump_capabilities.txt
   63  stat /data/mephi-web > ~/permissions.txt
   64  id mephi-admin > ~/users_groups.txt
   65  getent group mephi-devs >> ~/users_groups.txt
   66  curl -s http://192.168.1.100 > ~/curl_output.txt
   67  cp /data/mephi-web/index.html ~/
   68  cp /tmp/tcpdump*.rpm ~/tcpdump.rpm
   69  history > ~/project_history.txt